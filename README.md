reads in a sequence of characters and stores each character in a deque;
displays the deque contents;
uses a second deque to store the characters in reverse order;
displays the contents of both deques.
